# TIoS_UDAADB
Database and general Management bot for TIoS

Managed by N_Plasma


Note : Currently the bot token variable is in UDAADB_Core.py, it is not a actual token so until QS can automatically set bot token it has to be manually added
